# Features

Per-feature design docs for {{REPO_NAME}}. One markdown file per feature
(`{feature}.md`), created when a feature needs more than a TASKS.md line — its
own design, edge cases, and acceptance criteria.

- Use the `feature-planning` skill for comprehensive plans, or the
  `prd-workflow` skill to plan **and** implement.
- Keep each doc focused on one feature. Cross-link the relevant ADRs in
  [`../decisions/`](../decisions/).

| Feature | Status | Doc |
|---------|--------|-----|
| _none yet_ | — | — |
